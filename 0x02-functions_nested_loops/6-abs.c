#include <stdlib.h>

/**
 * _abs - integer positive
 *
 * @numb: number to calculate absolute
 *
 * Dercription: Compute the absolute of a number
 *
 * Return: numb if numb greater 0 else -numb
 */
int _abs(int numb)
{
	return (abs(numb));
}
